describe('Client tests', () => {
  it('renders without crashing', () => {
    cy.visit('/')
    cy.contains('Sauti Market Prices API') // delete this dummy test after we write real client code

    /*=== Add more tests here ===*/
  })
})
