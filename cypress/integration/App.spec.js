/* eslint-disable no-undef */
describe('Client tests', () => {
  it('renders without crashing', () => {
    cy.visit('/')
    cy.contains('Sauti')
    // delete this dummy test after we write real client code
    /*=== Add more tests here ===*/
  })
})
